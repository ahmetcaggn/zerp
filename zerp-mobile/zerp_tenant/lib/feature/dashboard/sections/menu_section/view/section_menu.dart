import 'package:flutter/material.dart';

class SectionMenu extends StatelessWidget {
  const SectionMenu({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Menu Section')),
    );
  }
}
