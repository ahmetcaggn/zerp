server.port=8080

eureka.client.service-url.defaultZone=${EUREKA_DEFAULT_ZONE:http://eureka:8761/eureka/}

# Avoid Prometheus collisions between gateway route tags and default client-request tags.
management.observations.http.client.requests.name=gateway.http.client.requests
